// Use GET request to retrieve search query in the URL
import { NextResponse } from "next/server";

// Use search query to look through suburb_name in database and render specific suburb data
// export async function GET(request: Request) {
//     const { searchParams } = new URL(request.url);
// }
